{
    "Configuration": {
        "ApiUrl": null, // mandatory! fill in specific appSettings.{environment}.json
        "StsUrl": null, // mandatory! fill in specific appSettings.{environment}.json
        "StsClient": {
            "ClientId": "NSGHSM-spa",
            "RedirectUrl": null, // fill if different from SPA base url only
            "PostLogoutRedirectUri": null, // fill if different from SPA base url only
            "ResponseType": "id_token token",
            "Scope": "openid profile NSGHSM-api",
            "SilentRenew": true,
            "SilentRenewUrl": null, // fill if different from SPA base url + '/silent-renew.html' only
            "OAuthTokenValidationTimeOut": null //null means default value (3 sec.)
        }
    },
    "Logging": {
        "LogLevel": {
            "Default": "Warning"
        }
    }
}